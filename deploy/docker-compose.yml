version: "3.9"

services:
  math-logic-mcp:
    build:
      context: ..
      dockerfile: Dockerfile
    container_name: math-logic-mcp
    restart: unless-stopped
    # Default: stdio transport (use with MCP client piping)
    # For HTTP transport, uncomment:
    # command: ["math-logic-mcp", "--http"]
    # ports:
    #   - "8000:8000"
